searcher:
  api:
    host: localhost
    port: 8080

schema:
  movies:
    fields:
      title:
        type: text
        search: semantic
      overview:
        type: text
        search: semantic
